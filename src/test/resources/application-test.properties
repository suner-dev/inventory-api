spring.datasource.url=${TEST_DB_URL:jdbc:postgresql://localhost:5432/inventorydb_test}
spring.datasource.username=${TEST_DB_USERNAME:inventory}
spring.datasource.password=${TEST_DB_PASSWORD:inventory}
spring.jpa.hibernate.ddl-auto=none
spring.jpa.open-in-view=false
inventory.low-stock-threshold=5
